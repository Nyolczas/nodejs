// üzenet kiírása a konzolra
// @param message: string
var showConsoleMessage = function( message) {
    
    // Konzolra írás
    console.log(message);
};

// Exportok (publikussá tétel)
module .exports = {
    "showConsoleMessage": showConsoleMessage //nem muszály ugyanaannak a névnek lennie - a kulcs a lényeg.
};